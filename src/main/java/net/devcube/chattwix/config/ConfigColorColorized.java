package net.devcube.chattwix.config;

import fi.dy.masa.malilib.config.options.ConfigColor;

public class ConfigColorColorized extends ConfigColor {
    public ConfigColorColorized(String name, String defaultValue, String comment) {
        super(name, defaultValue, comment);
    }
}
